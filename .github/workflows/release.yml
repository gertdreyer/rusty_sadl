name: Rust

on:
  release:
    types:
      - created

env:
  CARGO_TERM_COLOR: always

jobs:
  build-linux:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: cargo build -Zmultitarget --target x86_64-pc-windows-gnu --target x86_64-unknown-linux-gnu --verbose --release
    - name: Generate Headers
      run: cargo test --features c-headers -- generate_headers
    - name: Archive production artifacts
      uses: actions/upload-artifact@v2
      with:
        name: release-linux
        path: |
          examples/
          librusty_sadl.h
          target/release/librusty_sadl.so
          target/release/rusty_sadl.dll
